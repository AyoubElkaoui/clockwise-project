// Middleware removed - route protection will be implemented in components
// This is a workaround since middleware is not working in Docker environment

export function middleware() {
  // No middleware - protection handled in components
  return;
}
