{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "ConnectionStrings": {
    // Use the local Firebird file that lives in the repository (database/atrium_mvp.fdb)
    // When running inside Docker compose the backend overrides this via env vars to /firebird/data/atrium_mvp.fdb
    // Local dev: use embedded Firebird (no network hop) against the repo DB file
    "Firebird": "Database=/home/ayoub/clockwise-project/database/atrium_mvp.fdb;User=SYSDBA;Password=masterkey;Dialect=3;Charset=UTF8;ServerType=1;Pooling=true;ClientLibrary=/usr/lib64/libfbclient.so.2;Auth=Legacy",
    "Postgres": "Host=localhost;Database=dummy",
    "DefaultConnection": "Server=localhost;Database=atrium_mvp.fdb;User=SYSDBA;Password=masterkey;Dialect=3;Charset=UTF8;Port=3050;CreateDatabase=true;"
  }
}
